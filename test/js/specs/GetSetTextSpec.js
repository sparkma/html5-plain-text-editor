describe('get-set-text-spec', function(){   /**   * Shortcut to PlainTextEditor obj   */   var pte = window.plainTextEditor;    /**   * Properties for a target div   */   var _content = "c'est \n tres belle";   var _id = "test-target-el";      /**   * Adds a new div that is a target for   * PlainText editor before each test   */   beforeEach(function() {            $("body")            .first()            .append("<div id=\""                      + _id                      + "\">"                      + _content                      + "</div>");   });      /**   * Removes newly added div after each test   */   afterEach(function() {      $("#" + _id ).remove();   });      /**   * Tests are listed below:   */   it("returns whole text content", function() {      var el = $("#" + _id).get(0);      pte.bindToElem(el);            var txt = pte.getText();            expect(txt).toBe(_content);            expect(txt).not.toBe("bad");   });      it("overrides text with a past value", function() {      var el = $("#" + _id).get(0);      pte.bindToElem(el);            var txt = pte.getText();      expect(txt).toBe(_content);            var newContent = "new \n content";      pte.setText(newContent);       txt = pte.getText();      expect(txt).toBe(newContent);      expect(txt).not.toBe(_content);   });   });